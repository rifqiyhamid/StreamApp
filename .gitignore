# Dependencies
node_modules/

# Database file (jika Anda tidak ingin menyimpannya di Git)
streaming_app.db 
# Jika Anda ingin memulai dengan DB kosong di server atau mengelolanya secara terpisah, abaikan ini.
# Jika Anda ingin DB ikut tersinkron (untuk data awal development), jangan masukkan ke .gitignore.

# Uploads (video dan thumbnail pengguna biasanya tidak di-commit)
uploads/

# Log files
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pm2.log*
ecosystem.config.js.log* # Jika ada log spesifik PM2

# Environment variables file
.env 
# Jika Anda menggunakan file .env untuk menyimpan konfigurasi sensitif

# OS generated files
.DS_Store
Thumbs.db
